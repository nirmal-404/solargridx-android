<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />
    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.SolarGridX">
        <meta-data android:name="com.google.android.geo.API_KEY" android:value="${MAPS_API_KEY}" />
        <activity
            android:name=".ui.LoginActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.MainActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />
        <!-- Member 2: station node map screen -->
        <activity
            android:name=".ui.MapsActivity"
            android:exported="false"
            android:label="Node Map" />
        <!-- QR Dispatcher screen -->
        <activity
            android:name=".ui.QrDispatcherActivity"
            android:exported="false"
            android:label="QR Dispatcher"
            android:windowSoftInputMode="adjustResize" />
        <!-- Create Reservation screen -->
        <activity
            android:name=".ui.CreateReservationActivity"
            android:exported="false"
            android:label="Book Energy Slot"
            android:windowSoftInputMode="adjustResize" />
        <!-- Prosumer Registration screen -->
        <activity
            android:name=".ui.RegisterActivity"
            android:exported="false"
            android:label="Prosumer Registration"
            android:windowSoftInputMode="adjustResize" />
        <!-- Prosumer Profile & Account Control screen -->
        <activity
            android:name=".ui.ProfileActivity"
            android:exported="false"
            android:label="Account Profile"
            android:windowSoftInputMode="adjustResize" />
        <!-- Bookings Queue, History & Search screen -->
        <activity
            android:name=".ui.ReservationsActivity"
            android:exported="false"
            android:label="Microgrid Bookings"
            android:windowSoftInputMode="adjustResize" />
    </application>
</manifest>